# Ren'Py 汉化工具 — AI 编程助手全局上下文

你是本项目的核心开发者。每次对话前请通读本文档，掌握项目全貌后再动手。

---

## 1. 项目定位

一个**纯 Python（零第三方依赖）** 的 Ren'Py 游戏自动汉化工具，通过 LLM API 将 .rpy 脚本翻译为简体中文。

**核心能力**：
- 五大 LLM 提供商统一接口（xAI / OpenAI / DeepSeek / Claude / Gemini）
- 三种翻译模式：direct-mode / tl-mode / retranslate 补翻
- 50+ 项结构化翻译校验
- 断点续传 + 并发翻译
- 四阶段一键流水线（试跑→闸门→全量→补翻）
- 占位符保护/还原 + 术语锁定 + 翻译记忆
- chunk 级自动重试 + 结构化 logging 系统
- AI 自动术语表提取（pilot 后自动生成术语表）
- Tkinter 图形界面（gui.py）+ PyInstaller 打包为单文件 .exe（build.py）

**技术栈**：Python ≥ 3.9，纯标准库（threading / json / re / pathlib / urllib / logging / csv / tkinter），~12,000 行核心代码。代码按职责组织为 `core/`（基础设施）、`translators/`（翻译引擎）、`tools/`（辅助工具）、`engines/`（多引擎架构）、`file_processor/`（文件处理）、`pipeline/`（流水线）六个包。

**多引擎架构**（阶段一已完成）：
- `EngineProfile` 参数化引擎差异 + `TranslatableUnit` 通用文本单元 + `EngineBase` ABC
- `EngineDetector` 自动检测（Ren'Py / RPG Maker MV/MZ / VX/Ace）+ `--engine` CLI 路由
- `RenPyEngine` 薄包装委托现有三条管线，零改动

---

## 2. 项目位置与参考资源

**本项目**：`C:\Users\16097\Desktop\Renpy翻译\Renpy汉化（我的）`

**参考项目**（位于 `C:\Users\16097\Desktop\Renpy翻译\` 下）：
| 项目 | 参考价值 |
|------|----------|
| AiNiee-main | 插件化检查链、CoT 翻译、多后端适配（⭐5.3k） |
| LinguaGacha-main | TextProcessor 占位符保护、自动术语表、Force Thinking |
| projz_renpy_translation-devp | AST 级定位+回写、条目 ID 驱动、I18N 插件 |
| renpy-translator-main | 特殊符号保护、Runtime Hook 提取、Translation Mod 打包 |
| renpy-chinese-tl-main | 最佳实践文档、renpy-ttk 工作流、tl/none 技巧 |
| CN_Patch.Being_A_DIK-main | 成品补丁工程参考 |
| cross-channel_chinese-localization_project-master | 深度 UI 定制、全平台移植 |

> 借鉴参考项目时，先完整阅读其源码再给方案。

---

## 3. 文件职责总览

### 3.1 入口与编排

| 文件 | 行数 | 职责 |
|------|------|------|
| **main.py** | ~295 | CLI 入口 + argparse + `setup_logging` + 路由（`--engine auto/renpy` 走原路径；其他走 `resolve_engine()`）+ `--tl-mode` / `--retranslate` 互斥检查 + 向后兼容 re-export + CLI 参数校验 + `--no-clean-rpyc` / `--engine` / `--tl-screen` 参数 |
| **one_click_pipeline.py** | ~176 | 四阶段编排器（薄 facade）。`StageError` / 常量 / `parse_args()` / `main()` + 从 pipeline/ 和 translators/renpy_text_utils 重导出全部公共 API（保持向后兼容） |
| **pipeline/__init__.py** | ~26 | 一键流水线拆分包 re-export |
| **pipeline/helpers.py** | ~126 | 流水线辅助函数：`_print` / `resolve_scan_root` / `list_rpy_files` / `score_file` / `pick_pilot_files` / `run_main` / `package_output` |
| **pipeline/gate.py** | ~520 | 闸门评估：`evaluate_gate` / `attribute_untranslated` / `collect_strings_stats` / `write_report_summary_md` |
| **pipeline/stages.py** | ~442 | 流水线阶段：`_run_pilot_phase` / `_run_full_translation_phase` / `_run_retranslate_phase` / `_run_tl_mode_phase` / `_run_final_report` |

### 3.2 core/ 基础设施包

| 文件 | 行数 | 职责 |
|------|------|------|
| **core/api_client.py** | ~485 | 多提供商 API 抽象层。`APIClient` / `APIConfig` 统一接口（推理模型自动 timeout 提升 300s）、`RateLimiter`（RPM/RPS 线程安全）、`UsageStats` token+成本追踪、6 级 JSON 解析容错、指数退避重试（jitter + Retry-After + 404/401 立即报错不重试）、精确定价表 |
| **core/config.py** | ~170 | 配置文件管理：Config 类（三层合并 CLI>配置文件>DEFAULTS）+ resolve_api_key（env/file）+ `validate()` 配置校验（类型/范围/未知键告警） |
| **core/lang_config.py** | ~130 | 目标语言抽象层：LanguageConfig dataclass + 4 预置语言（zh/ja/ko/zh-tw）+ 文字检测 + 兼容字段读取 |
| **core/glossary.py** | ~470 | 术语表管理。`Glossary` 类：`scan_game_directory` Ren'Py 角色提取、`scan_rpgmaker_database` RPG Maker 角色名+系统术语提取（Actors.json + System.json terms）、`load_dict` CSV/JSONL 词典、`locked_terms` / `no_translate` 术语控制、`update_from_translations` 翻译记忆（`_memory_count` 信心度追踪，count>=2 才输出 prompt）、`to_prompt_text` 格式化输出、`extract_terms_from_translations` AI 自动术语表提取、`get_consistent_translation`（术语一致性查询）/ `_evict_low_frequency`（内存上限淘汰）/ `MAX_MEMORY_ENTRIES=10000` |
| **core/prompts.py** | ~529 | Prompt 模板工厂。direct/retranslate/tl-mode 三套模板；`_ENGINE_PROMPT_ADDONS`（rpgmaker/generic）；`_COT_ADDON_ZH` / `_COT_ADDON_EN` CoT 思维链 addon；`build_system_prompt(engine_profile=, cot=)` + `build_tl_system_prompt(cot=)` 参数化 |
| **core/translation_db.py** | ~124 | `TranslationDB` 类。逐行翻译元数据存储（file/line/original/translation/status/error_codes/warning_codes/provider/model/run_id）、upsert 去重、`has_entry()` 公共查询、按 status/file/stage 筛选 |
| **core/translation_utils.py** | ~310 | 公共辅助。`ChunkResult` / `ProgressTracker`（批量写入+损坏容错）/ `_restore_placeholders_in_translations` / `_strip_char_prefix` / `_filter_checked_translations` / `_deduplicate_translations` / `_build_fallback_dicts` / `_match_string_entry_fallback` / TranslationCache（会话级翻译缓存）/ 常量 |

### 3.3 translators/ 翻译引擎包

| 文件 | 行数 | 职责 |
|------|------|------|
| **translators/direct.py** | ~960 | direct-mode 翻译引擎。`translate_file`（密度路由 + chunk 翻译）、`_translate_file_targeted`（低密度定向翻译）、`run_pipeline`（全量翻译流水线 + dry-run + 断点续传）、`_should_retry`（截断检测返回 `(should, needs_split)` 元组）、`_split_chunk`（三级优先拆分：label 边界 > 空行 > 二等分）、`_translate_chunk_with_retry`（截断时拆分重试 + 结果合并）、dry-run verbose 增强（`_compute_file_dialogue_stats` / `_print_density_histogram` / `_print_term_scan_preview`）。注：原 direct_translator_dryrun.py 已合并至此模块 |
| **translators/retranslator.py** | ~487 | 补翻引擎。`retranslate_file` / `find_untranslated_lines` / `build_retranslate_chunks` / `calculate_dialogue_density` / `run_retranslate_pipeline` |
| **translators/tl_mode.py** | ~740 | tl-mode 引擎。`run_tl_pipeline` / `build_tl_chunks` / `_apply_tl_game_patches`（生成 `none_overlay.rpy` 覆盖模板，不直接修改 gui.rpy；支持 font_config.json）/ `_inject_language_buttons` / `_clean_rpyc`（全量清理 .rpyc/.rpymc/.rpyb；modified_files 模式精确清理三种后缀 + `--no-clean-rpyc`） |
| **translators/screen.py** | ~450 | screen 裸英文翻译器。`ScreenTextEntry` 数据类 + 四组正则（text/textbutton/tt.Action/Notify）+ `in_screen` 上下文检测 + `_should_skip` 跳过逻辑（标签剥离后检测文件路径）+ `_line_has_underscore_wrap` 跳过 `_()` + 去重翻译 + 逐行正则替换 + `.bak` 备份 + `screen_translate_progress.json` 断点续传 + dry-run + 51 个内建断言 |
| **translators/tl_parser.py** | ~1,090 | Ren'Py tl/ 框架独立解析器。状态机解析（IDLE/DIALOGUE/STRINGS/SKIP）、`DialogueEntry` / `StringEntry` / `TlParseResult` 数据类、`fill_translation` 行级精确回填（含引号剥离保护）、`postprocess_tl_file` / `postprocess_tl_directory` 后处理、`fix_nvl_translation_ids` / `fix_nvl_ids_directory` nvl clear 翻译块 ID 修正（Ren'Py 8.6+ say-only → 7.x nvl+say 哈希）、75 个内建测试 |
| **translators/renpy_text_utils.py** | ~83 | Ren'Py 文本分析公共函数。从 one_click_pipeline.py 提取：`_is_user_visible_string_line` / `_is_untranslated_dialogue` / `_extract_dialogue_text` / `count_untranslated_dialogues_in_file` / `collect_untranslated_details` |

### 3.4 file_processor/ 文件处理包

| 文件 | 行数 | 职责 |
|------|------|------|
| **file_processor/** | ~1,350 (4 sub-modules) | .rpy 文件处理核心 **包**。`splitter.py`（`split_file` 按块拆分）、`patcher.py`（`apply_translations` 行级回写）、`checker.py`（`protect_placeholders(patterns=)` 引擎参数化占位符保护 + `restore_placeholders` + `check_response_item(placeholder_re=)` 参数化校验 + `check_response_chunk` + `_extract_placeholder_sequence(regex=)` 参数化提取）、`validator.py`（`validate_translation` 主入口调度 4 个子检查函数）、`__init__.py` re-exports 全部公共 API、`SKIP_FILES_FOR_TRANSLATION` 跳过名单 |

### 3.5 engines/ 多引擎架构包

| 文件 | 行数 | 职责 |
|------|------|------|
| **engines/__init__.py** | ~39 | 引擎包公共 API re-export（EngineProfile / TranslatableUnit / EngineBase / EngineType / resolve_engine 等） |
| **engines/engine_base.py** | ~202 | `EngineProfile`（9 字段）+ `TranslatableUnit`（8 字段）+ `EngineBase` ABC + 内置 Profile 常量 + `ENGINE_PROFILES` 注册表 |
| **engines/engine_detector.py** | ~160 | `EngineType` 枚举（6 值）+ `detect_engine_type`（5 级优先）+ `create_engine` 延迟导入 + `resolve_engine` CLI 路由 |
| **engines/generic_pipeline.py** | ~414 | 通用翻译流水线 6 阶段（提取→初始化→分块→并发翻译→回写→报告）+ 断点恢复 + RPG Maker 术语钩子 |
| **engines/renpy_engine.py** | ~74 | Ren'Py 薄包装。`run` 路由到三条管线（延迟导入） |
| **engines/rpgmaker_engine.py** | ~636 | RPG Maker MV/MZ。事件指令(401/405 合并+102 选项+320 改名) + 8 种数据库 + System.json + JSON path 回写 |
| **engines/csv_engine.py** | ~317 | CSV/JSONL 通用格式。列名别名(6 组) + UTF-8 BOM + 多行 CSV + 按源格式分流回写 |

### 3.6 tools/ 辅助工具包 & 界面

| 文件 | 职责 |
|------|------|
| **tools/font_patch.py** (~147行) | `resolve_font` 字体查找 + `load_font_config` 加载 font_config.json + `apply_font_patch(font_config_path=)` 字体+字号/布局参数补丁 |
| **tools/renpy_upgrade_tool.py** (~17K) | Ren'Py 7→8 升级扫描器。19 条规则（Python 2→3 + 废弃 API）、`--fix` 自动修复、`--backup` 备份、rpyc 清理、RENPY-020 show 空 ATL 块修复（多行感知） |
| **tools/review_generator.py** (~150行) | HTML 翻译校对报告（side-by-side 原文/译文，error/warning 高亮，按文件折叠） |
| **tools/verify_alignment.py** (~171行) | 零 API 验证工具，测试新校验策略 |
| **tools/revalidate.py** (~54行) | 对已翻译输出重跑闸门检查（不调 API） |
| **tools/patch_font_now.py** (~40行) | 翻译完成后独立运行字体补丁 |
| **tools/analyze_writeback_failures.py** (~120行) | 回写失败根因分析工具（WF-01~08 分类统计） |
| **gui.py** (~730行) | Tkinter 图形界面启动器。3 Tab（基本设置 + 翻译设置动态引擎面板 + 高级设置）、引擎选择联动面板切换、API key 隐藏、命令预览（实时+****遮蔽）、内嵌日志（ScrolledText + queue 线程安全）、subprocess 执行/停止、配置 JSON 加载/保存、工具菜单（Dry-run 摘要弹窗 + 升级扫描）、DPI 适配 + 进度条（ttk.Progressbar + 百分比标签）+ 自适应轮询（50/200ms）+ 日志裁剪（5000→3000 行）+ 优雅终止（CTRL_C_EVENT + wait） |
| **build.py** (~100行) | PyInstaller 打包脚本。35 个 hidden-import + 资源文件 → `dist/多引擎游戏汉化工具.exe`（~32MB 单文件） |
| **start_launcher.py** (~218行) | CLI 中文交互启动器（9 种模式菜单），`START.bat` 调用，`getpass` 隐藏 API Key 输入 |

### 3.7 测试

| 文件 | 职责 |
|------|------|
| **tests/test_all.py** (~1580行) | 综合模块测试（87 个用例：单元 + 集成 + screen translator） |
| **tests/test_engines.py** (~962行) | 引擎抽象层测试（62 个用例：EngineProfile 6 + TranslatableUnit 3 + Detector 10 + RenPyEngine 5 + EngineBase 1 + CSV 10 + generic_pipeline 5 + patcher/checker 4 + prompts 2 + write_back 1 + RPG Maker 15） |
| **tests/test_single.py** | 单文件端到端测试（需 API key） |
| **tests/smoke_test.py** | 校验逻辑冒烟测试（13 个用例） |
| **tests/fixtures/** | 测试用的 .rpy 样本文件 |

### 3.8 配置与输出

| 文件/目录 | 说明 |
|-----------|------|
| **requirements.txt** | 空（纯标准库，无依赖） |
| **pyproject.toml** | 项目元数据（name/version/requires-python） |
| **LICENSE** | MIT 许可证 |
| **renpy_translate.example.json** | 示例配置文件模板 |
| **START.bat** | Windows 入口脚本 |
| **output/** | 翻译结果、glossary.json、translation_db.json、tl_progress.json、tl_mode_report.json |
| **resources/fonts/** | 内置中文字体（NotoSansSC / SourceHanSansSC） |

---

## 4. 翻译模式详解

### 4.1 Direct-mode（默认）

```
run_pipeline(args) 入口
  ├─ 初始化：APIClient / Glossary / ProgressTracker / TranslationDB
  ├─ 扫描 .rpy → 排除引擎(renpy/lib/) → 排除 --exclude → tl-priority 过滤
  ├─ 按文件大小升序排列（小文件优先积累翻译记忆）
  └─ 逐文件翻译：
      translate_file() 入口（translators/direct.py）
        ├─ 文件名 ∈ SKIP_FILES_FOR_TRANSLATION → [SKIP-CFG] 直接复制
        ├─ calculate_dialogue_density() < 阈值 → _translate_file_targeted()（定向翻译）
        └─ 密度 ≥ 阈值 → split_file() → 逐 chunk 翻译：
            _translate_chunk_with_retry()（自动重试；截断时自动拆分重试）
              ├─ _should_retry() → (should, needs_split) 元组
              ├─ needs_split=True → _split_chunk()（label 边界 > 空行 > 二等分）→ 分别翻译 → 合并
              └─ _translate_chunk()（translators/direct.py 模块级函数）
                    ├─ prev_context: 前一 chunk 末尾 5 行（提供上下文连贯性）
                    ├─ protect_placeholders() → __RENPY_PH_N__ 令牌
                    ├─ client.translate()（API 调用）
                    ├─ restore_placeholders()
                    ├─ check_response_item() + check_response_chunk()（ResponseChecker）
                    └─ 不通过 → 丢弃保留原文，status="checker_dropped"
                    → ChunkResult dataclass 封装返回值（core/translation_utils.py）
      apply_translations()（行级回写，四遍匹配策略；第四遍全文扫描时跳过已被前面 pass 修改的行（`modified_lines` + `full_scan` 参数），防止同一原文多次出现时误替换）
      validate_translation()（50+ 项校验）
      glossary.update_from_translations()（翻译记忆学习）
```

**密度自适应路由**：
- 阈值：`--min-dialogue-density`，默认 0.20
- 低密度文件（多代码少对话）→ 提取对话行 + ±3 行上下文 → 小 chunk 定向翻译
- 高密度文件 → 整文件按块拆分 → 全量翻译

### 4.2 tl-mode（`--tl-mode --tl-lang chinese`）

```
run_tl_pipeline(args) 入口（translators/tl_mode.py）
  ├─ scan_tl_directory() 扫描 tl/<lang>/*.rpy（排除 common.rpy）
  ├─ get_untranslated_entries() 筛选空槽位
  │   ├─ DialogueEntry: translation == ""
  │   └─ StringEntry: new == ""
  ├─ build_tl_chunks()：按文件分组，每 chunk ≤ 30 条
  │   └─ 含 `\n` 的多行条目自动添加 `[MULTILINE]` 标记
  ├─ ThreadPoolExecutor 并发翻译（--workers 控制线程数）
  │   └─ per-chunk: protect_placeholders → API → restore_placeholders(id/original/zh) → check_response_item
  ├─ 匹配阶段：
  │   ├─ DialogueEntry: identifier hash 精确匹配
  │   └─ StringEntry: 四层 fallback（精确 → strip → 去占位符令牌 → 转义规范化）
  └─ fill_translation() 行级精确回填
      └─ str.replace 只替换第一个 "" → "译文"（保留缩进/character 前缀）
      └─ 引号剥离保护：ASCII ""、弯引号 ""、全角 ＂＂
  └─ postprocess_tl_directory()：移除 nvl clear 语句 + 补 pass
  └─ fix_nvl_ids_directory()：修正 nvl clear 翻译块 ID（8.6+ say-only → 7.x nvl+say 哈希）
```

**关键设计**：
- translators/tl_parser.py 独立模块，状态机解析（IDLE/DIALOGUE/STRINGS/SKIP），UTF-8 BOM 处理
- 回填精度远高于 direct-mode（行号精确定位 vs 文本匹配回写），可消除"回写失败"类漏翻
- .bak 备份（首次回填前创建，不覆盖已有备份）
- 独立进度文件 tl_progress.json，chunk 级断点续传
- 并发安全：RateLimiter / UsageStats / ProgressTracker 均自带 threading.Lock
- nvl clear ID 修正：Ren'Py 8.6+ 生成的 .tl 模板用 say-only 哈希，但 7.x 引擎用 nvl+say 哈希，后处理自动检测并修正

### 4.3 Retranslate 补翻模式（`--retranslate`）

```
retranslate_file()（translators/retranslator.py）
  ├─ find_untranslated_lines()：检测残留英文行
  │   └─ 排除 auto/hover/idle 定义行、image 路径行、screen 布局属性行
  ├─ build_retranslate_chunks()：每 chunk ≤ 20 行漏翻行 + ±3 行上下文，合并重叠区间
  ├─ 专用 retranslate prompt（标记 >>> 行必须翻译，上下文行仅供参考）
  ├─ 原地补翻，.bak 自动备份（不覆盖已有备份）
  └─ 独立进度文件 retranslate_progress.json
```

---

## 5. 四阶段一键流水线（one_click_pipeline.py）

```
main() 纯编排入口（~113 行）
  │
  ├─ (if tl-mode) → _run_tl_mode_phase()
  │     跳过试跑，直接 tl 全量翻译 → 读取报告
  │
  └─ (if direct-mode)
      ├─ Stage 1/4 → _run_pilot_phase()
      │   pick_pilot_files(): 按风险评分选 20 个文件
      │   score = min(size/1KB, 200) + [80 if risk_keyword] + [30 if sazmod]
      │   → run_main() 翻译 → evaluate_gate() → AI 术语提取
      │   → errors > 0 则中止；ratio 超阈值则警告继续
      │
      ├─ Stage 2/4 → _run_full_translation_phase()
      │   run_main() 翻译全部文件 → evaluate_gate()
      │   → errors > 0 则中止
      │
      ├─ Stage 3/4 → _run_retranslate_phase()
      │   collect_files_with_untranslated() → retranslate_file() × N
      │   → 原地补翻残留英文行
      │
      └─ Stage 4/4 → _run_final_report()
          evaluate_gate() 最终评估
          attribute_untranslated() 漏翻归因
          write_report_summary_md() 生成报告
          package_output() → zip 打包

目录结构：
  output/projects/<project_name>/
    ├─ stage0_raw/          (预留)
    ├─ stage1_normalized/   (预留)
    ├─ stage2_translated/   (全量翻译结果)
    ├─ stage3_polished/     (预留)
    └─ _pipeline/
        ├─ pilot_input/     (试跑输入)
        ├─ pilot_output/    (试跑输出)
        └─ *.log
```

---

## 6. 翻译质量保障链

### 6.1 发送前保护

```
protect_placeholders()
  [name] → __RENPY_PH_0__
  {color=#f00} → __RENPY_PH_1__
  %(var)s → __RENPY_PH_2__
  → 按出现顺序全局去重，生成映射 list[(index, original)]
```

### 6.2 返回后校验（ResponseChecker）

```
check_response_item():
  ├─ 占位符集合一致性（原文 vs 译文）
  ├─ 原文非空 + 译文非空
  └─ 不通过 → status="checker_dropped"，保留原文

check_response_chunk():
  └─ 条数一致性（启发式 _count_translatable_lines_in_chunk）
```

### 6.3 回写后校验（validate_translation，50+ 项）

| 类别 | 检查项 | Code |
|------|--------|------|
| 结构 | 行数一致性 | — |
| 结构 | 缩进保留 | — |
| 结构 | 关键字保留（label/screen/jump/call 等） | — |
| 变量 | `[var]` 缺失 | E210_VAR_MISSING |
| 变量 | `[var]` 多余 | W211_VAR_EXTRA |
| 标签 | `{tag}` 配对不一致 | E220_TEXT_TAG_MISMATCH |
| 菜单 | `{#id}` 标识符不一致 | E230_MENU_ID_MISMATCH |
| 格式化 | `%(name)s` 占位符不一致 | E240_FMT_PLACEHOLDER_MISMATCH |
| 顺序 | 占位符顺序偏差（仅 warning，仍 apply） | W251_PLACEHOLDER_ORDER |
| 术语 | 锁定术语未使用规定译名 | E411_GLOSSARY_LOCK_MISS |
| 术语 | 禁翻片段被修改 | E420_NO_TRANSLATE_CHANGED |
| 长度 | 译文长度比例异常（<0.15 或 >2.5） | W430_LEN_RATIO_SUSPECT |
| 风格 | 模型自我描述片段 | W440_MODEL_SPEAKING |
| 风格 | 中英标点连续混用 | W441_PUNCT_MIX |
| 风格 | 中文字符占比极低 | W442_SUSPECT_ENGLISH_OUTPUT |
| 控制标签 | Ren'Py 控制标签 ({w}/{p}/{nw}/{fast}/{cps=N}) 损坏 | E250_CONTROL_TAG_DAMAGED |
| 过度翻译 | Ren'Py 关键字（label/screen 等）被翻译成中文 | W460_POSSIBLE_OVERTRANSLATION |
| 标点 | 连续中文标点（。。、！！） | W470_CONSECUTIVE_PUNCTUATION |

### 6.4 数据采集与归因

- **per-chunk 指标**：report.json → chunk_stats（expected / returned / dropped 三元组）
- **漏翻归因**：`attribute_untranslated()` → 四分类：
  - AI 未返回（最大占比 ~71%）
  - 回写失败（~28%）
  - Checker 丢弃（~1%）
  - 未知
- **translation_db** 记录每条翻译的完整元数据，支持增量归因查询

---

## 7. API 集成

### 7.1 支持的提供商与定价

| 提供商 | 端点 | 默认模型 | 输入/输出 ($/M tokens) |
|--------|------|----------|----------------------|
| xAI | api.x.ai/v1 | grok-4-1-fast-reasoning | 0.20 / 0.50 |
| OpenAI | api.openai.com/v1 | gpt-4o-mini | 0.15 / 0.60 |
| DeepSeek | api.deepseek.com/v1 | deepseek-chat | 0.14 / 0.28 |
| Claude | api.anthropic.com/v1 | claude-sonnet-4 | 3.00 / 15.00 |
| Gemini | generativelanguage.googleapis.com | gemini-2.5-flash | 0.15 / 0.60 |

### 7.2 JSON 解析容错链（6 级降级）

1. 直接 `json.loads()`
2. 从 Markdown 代码块提取 ` ```json...``` `
3. 搜索第一个 `[` 到最后一个 `]`
4. 修复尾部逗号
5. 正则逐项提取 JSON 对象
6. 字段顺序容错

### 7.3 速率控制

- `RateLimiter`：线程安全的 RPM + RPS 双重限制
- 指数退避重试：429/5xx 错误自动重试，jitter 随机抖动避免惊群，优先读取 `Retry-After` 响应头，退避上限 60s
- `UsageStats`：线程安全的 token 用量 + 费用实时统计
- 推理模型检测：reasoning model 的 thinking tokens 按 3-5× 计费

---

## 8. CLI 参数速查

### 8.1 main.py

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--game-dir` | (必填) | 游戏根目录 |
| `--output-dir` | `output` | 输出目录 |
| `--provider` | (必填) | xai/openai/deepseek/claude/gemini |
| `--api-key` | `""` | API 密钥（dry-run 可不填） |
| `--model` | `""` | 模型名（留空用默认） |
| `--genre` | `adult` | 风格：adult/visual_novel/rpg/general |
| `--rpm` | `60` | 每分钟请求数 |
| `--rps` | `5` | 每秒请求数 |
| `--timeout` | `180.0` | API 超时秒数 |
| `--temperature` | `0.1` | 生成温度 |
| `--max-chunk-tokens` | `4000` | 每 chunk 最大 token 数 |
| `--max-response-tokens` | `32768` | 最大响应 token 数 |
| `--workers` | `1` | 并发线程数 |
| `--resume` | `false` | 断点续传 |
| `--dry-run` | `false` | 仅扫描估费 |
| `--retranslate` | `false` | 补翻模式 |
| `--tl-mode` | `false` | tl 翻译模式 |
| `--tl-lang` | `chinese` | tl 语言子目录名 |
| `--tl-priority` | `false` | 仅翻译 tl/ 下的脚本 |
| `--min-dialogue-density` | `0.20` | 低密度阈值 |
| `--dict` | `[]` | 外部词典路径 |
| `--exclude` | `[]` | 排除文件 glob 模式 |
| `--copy-assets` | `false` | 复制非 .rpy 资源 |
| `--patch-font` | `false` | 自动字体补丁 |
| `--font-file` | `""` | 指定字体文件 |
| `--target-lang` | `zh` | 目标语言 |
| `--stage` | `single` | 阶段标记（pipeline 内部用） |
| `--log-file` | `""` | 日志文件路径 |
| `--input-price` | `None` | 自定义输入价格 |
| `--output-price` | `None` | 自定义输出价格 |
| `--verbose` | `false` | 详细日志输出（DEBUG 级别） |
| `--quiet` | `false` | 安静模式（仅 WARNING 及以上） |
| `--no-clean-rpyc` | `false` | 跳过 tl-mode 翻译后的 .rpyc 缓存清理 |
| `--tl-screen` | `false` | 翻译 screen 中的裸英文字符串（text/textbutton/tt.Action），可与 `--tl-mode` 联用 |
| `--engine` | `auto` | 游戏引擎类型（auto/renpy/rpgmaker/csv/jsonl） |
| `--cot` | `false` | CoT 思维链翻译（直译→校正→意译，质量更高费用+30-50%） |
| `--font-config` | `""` | 字体配置文件路径（font_config.json） |

### 8.2 one_click_pipeline.py

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--pilot-count` | `20` | 试跑文件数 |
| `--gate-max-untranslated-ratio` | `0.08` | 闸门最大漏翻比 |
| `--clean-output` | `false` | 开始前清理输出 |
| `--package-name` | `CN_patch_game` | zip 包名 |
| `--tl-mode` | `false` | 使用 tl-mode 流水线替代 direct-mode |
| `--tl-lang` | `chinese` | tl 语言子目录名（配合 `--tl-mode`） |
| 其余参数同 main.py（透传） | | |

### 8.3 start_launcher.py 模式菜单

| 模式 | 说明 | 调用 |
|------|------|------|
| 1 | Ren'Py 主流程翻译（从头） | `main.py` |
| 2 | Ren'Py 主流程翻译（断点续跑） | `main.py --resume` |
| 3 | Ren'Py 仅扫描估费（Dry-run） | `main.py --dry-run` |
| 4 | Ren'Py 一键流水线 | `one_click_pipeline.py` |
| 5 | Ren'Py tl-mode 翻译（从头） | `main.py --tl-mode` |
| 6 | Ren'Py tl-mode 翻译（断点续跑） | `main.py --tl-mode --resume` |
| 7 | Ren'Py 7→8 升级扫描 | `tools/renpy_upgrade_tool.py` |
| 8 | RPG Maker MV/MZ 翻译 | `main.py --engine rpgmaker` |
| 9 | CSV/JSONL 通用格式翻译 | `main.py --engine csv/jsonl` |

---

## 9. 可配置常量速查

| 常量/参数 | 位置 | 默认值 | 说明 |
|-----------|------|--------|------|
| `SKIP_FILES_FOR_TRANSLATION` | file_processor/splitter.py | `{define, variables, screens, earlyoptions, options}.rpy` | 跳过翻译的配置文件 |
| `MODEL_SPEAKING_PATTERNS` | file_processor/checker.py | 7 条模式 | W440 模型自述检测关键词 |
| `PLACEHOLDER_ORDER_PATTERNS` | file_processor/checker.py | 4 组 (regex, name) | W251 占位符顺序提取 |
| `MIN_CHINESE_RATIO` | file_processor/validator.py | 0.05 | W442 中文占比阈值 |
| `CHECKER_DROP_RATIO_THRESHOLD` | core/translation_utils.py | 0.3 | chunk 丢弃率重试阈值 |
| `MIN_DROPPED_FOR_WARNING` | core/translation_utils.py | 3 | 最小丢弃数触发警告 |
| `MIN_DIALOGUE_LENGTH` | core/translation_utils.py | 4 | 定向翻译最小对话长度 |
| `LEN_RATIO_LOWER / UPPER` | one_click_pipeline.py | 0.15 / 2.5 | W430 长度异常阈值 |
| `RISK_KEYWORDS` | one_click_pipeline.py | 14 个关键词 | 试跑文件风险评分 |
| `MAX_FILE_RANK_SCORE` | one_click_pipeline.py | 200 | 文件大小评分上限 |
| `RISK_KEYWORD_SCORE` | one_click_pipeline.py | 80 | 风险关键词加分 |
| `SAZMOD_BONUS_SCORE` | one_click_pipeline.py | 30 | SAZMOD 模组额外加分 |
| `MIN_UNTRANSLATED_TEXT_LENGTH` | translators/renpy_text_utils.py | 20 | 漏翻检测最小文本长度 |
| `MIN_ENGLISH_CHARS_FOR_UNTRANSLATED` | translators/renpy_text_utils.py | 12 | 漏翻检测最小英文字符数 |
| `MAX_MEMORY_ENTRIES` | core/glossary.py | 10000 | 翻译记忆最大条目数 |
| `SAVE_INTERVAL` | core/translation_utils.py ProgressTracker | 10 | 批量写入间隔（每 N 次 mark 写磁盘） |
| `_PH_TOKEN_RE` | core/translation_utils.py | `__RENPY_PH_\d+__` | 占位符令牌正则（fallback 匹配用） |
| `_QUOTE_STRIP_PAIRS` | translators/tl_parser.py | ASCII "" / 弯引号 "" / 全角 ＂＂ | fill_translation 引号剥离对 |
| `_MODEL_PRICING` | core/api_client.py | ~20 个模型 | 精确定价表 |
| 截断匹配阈值 | file_processor/patcher.py line 414 | 0.7 | AI 截断文本匹配阈值（阶段 3e），低于此值不替换 |

---

## 10. Warning / Error Code 索引

| Code | 级别 | 含义 | 处理 |
|------|------|------|------|
| E210_VAR_MISSING | error | 译文缺少原文中的 `[var]` 变量 | 丢弃翻译 |
| W211_VAR_EXTRA | warning | 译文含原文没有的 `[var]` 变量 | 保留但告警 |
| E220_TEXT_TAG_MISMATCH | error | `{tag}` 配对不一致 | 丢弃翻译 |
| E230_MENU_ID_MISMATCH | error | `{#id}` 菜单标识符不一致 | 丢弃翻译 |
| E240_FMT_PLACEHOLDER_MISMATCH | error | `%(name)s` 格式化占位符不一致 | 丢弃翻译 |
| W251_PLACEHOLDER_ORDER | warning | 占位符顺序与原文不一致（集合相同） | 仅告警，仍 apply |
| W410_GLOSSARY_MISS | warning | 术语表未命中 | 告警 |
| E411_GLOSSARY_LOCK_MISS | error | 锁定术语未使用规定译名 | 标记错误 |
| E420_NO_TRANSLATE_CHANGED | error | 禁翻片段被修改 | 标记错误 |
| W430_LEN_RATIO_SUSPECT | warning | 译文长度比例异常 | 告警 |
| W440_MODEL_SPEAKING | warning | 模型自我描述/多余解释 | 告警 |
| W441_PUNCT_MIX | warning | 中英标点连续混用 | 告警 |
| W442_SUSPECT_ENGLISH_OUTPUT | warning | 中文占比极低，疑似未翻译 | 告警 |
| E250_CONTROL_TAG_DAMAGED | error | Ren'Py 控制标签 ({w}/{p}/{nw}/{fast}/{cps=N}) 在译文中缺失 | 标记错误 |
| W460_POSSIBLE_OVERTRANSLATION | warning | Ren'Py 关键字可能被过度翻译 | 告警 |
| W470_CONSECUTIVE_PUNCTUATION | warning | 连续中文标点（。。、！！） | 告警 |

---

## 11. 核心算法

### Token 估算
```python
token_count = ascii_chars // 4 + non_ascii_chars // 2 + 1
```

### 占位符保护
```
"[name] says: {color=#f00}Hello{/color}"
→ "__RENPY_PH_0__ says: __RENPY_PH_1__Hello__RENPY_PH_2__"
映射: [(0, "[name]"), (1, "{color=#f00}"), (2, "{/color}")]
```

### 文件拆分策略（split_file）
1. 识别顶层 Ren'Py 块边界（label / screen / define / init / translate / menu 等）
2. 按块累积 token，超过 `max_chunk_tokens`（默认 4000）时断开
3. 单块超限 → 按行数拆分，优先空行处断开
4. 每个 chunk 携带行偏移量（base_line_offset），确保多块重组

### 密度计算
```python
dialogue_density = dialogue_lines / non_empty_lines
if density < 0.20:  # 走定向翻译
```

### 风险评分（试跑文件选择）
```python
score = min(file_size / 1024, MAX_FILE_RANK_SCORE)
      + RISK_KEYWORD_SCORE * (any keyword in filename)
      + SAZMOD_BONUS_SCORE * ("sazmod" in filename)
```

### tl-mode StringEntry 四层 fallback 匹配
1. 精确匹配 `entry.old == api_returned_id`
2. strip 空白后匹配
3. 去占位符令牌后匹配
4. 转义规范化（`\"` → `"`, `\n` → 换行）后匹配

---

## 12. 验证数据（The Tyrant, ~140 文件）

### direct-mode 最终结果（密度自适应 + retranslate）
- **总漏翻率：4.01%**（2,165/54,045）
  - 主游戏：0.66%（104/15,681）
  - SAZMOD mod：5.37%（2,061/38,364）

### direct-mode 漏翻归因
| 归因 | 条数 | 占比 |
|------|------|------|
| AI 未返回 | 1,539 | 71.1% |
| 回写失败 | 609 | 28.1% |
| Checker 丢弃 | 17 | 0.8% |

### tl-mode 全量结果（107 文件, 10 线程）
- 总槽位：76,169（75,993 对话 + 176 字符串）
- 待翻译：74,044
- **翻译成功：74,019（99.97%）**
- 未翻译：25 条
- Checker 丢弃：4 条
- 费用：$2.73（grok-4-1-fast-reasoning，2,541 请求，73.1 分钟）
- 引号 bug：AI 约 14%（10,402/74,002）返回带引号译文，修复后 0 格式错误

### 历史漏翻率演进
| 阶段 | 漏翻率 | 关键改动 |
|------|--------|----------|
| 基线 | 12.12% | 无 |
| +占位符保护+checker | 10.40% | 挽回校验拦住的条目 |
| +密度自适应+retranslate | **4.01%** | 低密度文件定向翻译 |

### 关键发现
- 对话密度是漏翻率最强相关因子：<10% 密度文件漏翻中位数 57.69%，≥40% 仅 4.54%
- prompt 强化（CRITICAL RULE）无效甚至有害：降低返回率 5pp，已撤回
- tl-mode 回填精度远高于 direct-mode（行号精确定位 vs 文本匹配回写）
- AI 约 14% 概率返回带外层引号译文，fill_translation 需剥离

---

## 13. 已完成改进清单

### 第一轮：质量校验体系
1. W430 长度/密度异常告警
2. E411/E420 术语锁定与禁翻
3. W440/W441/W442 翻译风格规则检查
4. W251 占位符顺序校验
5. tl 优先模式（`--tl-priority`）
6. strings 统计视图（`collect_strings_stats`）
7. 闸门统计完整性补丁
8. core/prompts.py 花括号转义修复

### 第二轮：功能增强
9. README 组合式工作流推荐
10. 结构化报告 `report_summary.md`（三级判定绿/黄/红）
11. `translation_db.json` 增量翻译接口
12. 自动字体补丁（`--patch-font`）

### 第三轮：降低漏翻率（12.12% → 4.01%）
13. translate strings 专用回写路径
14. original 对齐（两层匹配：完全相等 + 前缀后缀≥0.8）
15. 占位符保护/还原（`__RENPY_PH_N__` 令牌）
16. ResponseChecker（`check_response_item` + `check_response_chunk`）
17. Checker 丢弃逻辑 + translation_db 记录
18. 数据采集框架（per-chunk 指标 + 漏翻归因）
19. 密度自适应翻译策略
20. 文件级排除名单（SKIP_FILES_FOR_TRANSLATION）
21. `find_untranslated_lines` 行级过滤
22. `--retranslate` 补翻模式
23. 原地补翻 .bak 备份

### 第四轮：tl-mode 翻译模式
24. translators/tl_parser.py 独立解析模块
25. `--tl-mode` + `--tl-lang` CLI 参数
26. tl-mode 专用 Prompt 模板
27. `build_tl_chunks` + `run_tl_pipeline`（含占位符保护/还原/checker 复用）
28. `fill_translation` 行级精确回填
29. id 字段占位符还原修复
30. StringEntry 四层 fallback 匹配
31. `run_tl_pipeline` 并发化（ThreadPoolExecutor）
32. start_launcher.py 新增模式 5/6

### 第五轮：tl-mode 全量验证 & 修复
33. `fill_translation` 引号剥离修复（三对引号自动剥离）

### 第六轮：工程化 & 健壮性
34. renpy_upgrade_tool RENPY-020 show 空 ATL 块修复（多行感知）
35. chunk 级自动重试（`_translate_chunk_with_retry`，最多 2 次重试）
36. logging 替代 print + `--verbose` / `--quiet` CLI 参数
37. file_processor.py 拆分为 `file_processor/` 包（splitter / patcher / checker / validator 四子模块 + `__init__.py` re-exports）
38. AI 自动术语表提取（`extract_terms_from_translations`，pilot 后自动生成术语表）
39. 429 退避优化（jitter 随机抖动 + Retry-After 响应头读取 + 60s 上限）

### 第七轮：精度提升 & 测试覆盖
40. 全文扫描安全性（第四遍跳过已修改行，防止同一原文多次出现时误替换）
41. tl_parser 内建测试新增 19 个断言（56→75），覆盖 `_sanitize_translation` 12 个边界 + `fill_translation` 7 个边界
42. 截断匹配阈值从 0.5 提高到 0.7（`_replace_string_in_line` 阶段 3e）

### 第八轮：代码质量八阶段优化
43. 8 处 except 精细化 + .tmp 清理（阶段一）
44. 消除代码重复：`_restore_placeholders_in_translations` / `_strip_char_prefix` / `_is_untranslated_dialogue` + `_extract_dialogue_text`（阶段二）
45. 删除 4 处冗余 `import re as _re`（阶段二）
46. 大函数拆分：`one_click_pipeline.main()` → `_run_retranslate_phase` + `_run_final_report`（阶段三）
47. `validate_translation` 293 行拆为 4 个子检查函数（阶段四）
48. Magic Number 收敛：10+ 处硬编码阈值提为命名常量（阶段六）
49. 测试体系升级：36→42 测试 + CI 增加 py_compile 步骤（阶段五）
50. 项目工程化：LICENSE + pyproject.toml + 4 处硬编码路径改环境变量（阶段七）
51. 6 处 except 精细化 + SKIP_TOKENS 常量（阶段八）

### 第九轮：深度优化六阶段
52. 并发锁修复（`n` → `n_completed` 局部变量）+ CLI 参数校验函数（`_positive_int`/`_positive_float`/`_ratio_float`）+ 词典文件提前校验 + ProgressTracker 损坏容错+规范化（阶段一）
53. `_filter_checked_translations` 提取(4处) + `_deduplicate_translations` 提取(3处)（阶段二）
54. `_build_fallback_dicts` + `_match_string_entry_fallback` O(n*m)→O(1) + ProgressTracker 批量写入 SAVE_INTERVAL=10（阶段三）
55. 空 choices 显式 warning + 404/401 不重试+友好提示 + 推理模型 auto timeout 300s（阶段四）
56. 新增 8 个测试(42→50)：progress resume/normalize/checker 过滤/去重/fallback/推理检测/参数校验/timeout（阶段五）
57. `TranslationDB.has_entry()` 封装 + 过期注释修正（阶段六）

### 第十轮：功能加固与生态完善六阶段
58. 确认控制标签 `{w}/{p}/{nw}/{fast}/{cps=N}/{done}` 已被现有 tag 正则覆盖 + 新增测试确认（阶段一）
59. API Key 用 `getpass` 隐藏输入 + 命令预览隐藏密钥（阶段二）
60. CI 增加零第三方依赖自动检查 + `--dry-run` 集成测试（阶段三）
61. Glossary 连字符人名正则 `Mary-Jane` + 翻译记忆信心度 `_memory_count`（count>=2 才输出 prompt）（阶段四）
62. README 新增：5 个提供商完整用法示例 + 性能调优表 + 5 项故障排查 FAQ（阶段五）
63. subprocess 调用 main.py 改绝对路径（跨平台兼容）（阶段六）

### 第十一轮阶段一：main.py 拆分 + 闭包重构
64. main.py 从 ~2400 行拆分为 5 个模块：main.py(234) / translators/direct.py / translators/retranslator.py / translators/tl_mode.py / core/translation_utils.py（原名 direct_translator.py / retranslator.py / tl_translator.py / translation_utils.py，后续重构迁入包）
65. main.py 保留 re-export 确保向后兼容
66. CI 增加 4 个新模块的 py_compile 检查
67. TranslationContext dataclass 替代闭包：`_translate_chunk` / `_translate_chunk_with_retry` / `_should_retry` / `_translate_one_tl_chunk` 从嵌套函数提升为模块级函数（现分别位于 translators/direct.py 和 translators/tl_mode.py）

### 第十一轮阶段二+三：回写失败分析与修复
68. `_diagnose_writeback_failure()` 诊断函数 + `apply_translations` 失败路径记录 `status="writeback_failed"` 到 DB
69. `analyze_writeback_failures.py` 分析工具（WF-01~08 分类统计 + JSON 报告）
70. `_replace_string_in_line` 阶段 1b：AI 返回含行前缀（`text _("` / `textbutton "`）的 original 时自动剥离
71. `_replace_string_in_line` 阶段 3 正则升级：支持转义引号 `\"` 的字符串匹配
72. `_replace_string_in_line` 阶段 3f：用剥离后 original 在 quoted_parts 中 fallback 匹配
73. 回写失败从 609 → 28 条（降 95%），进一步修复后预期接近 0

### 第十一轮阶段四：config.json 配置文件
74. `core/config.py`（~140行）：Config 类 + DEFAULTS 字典 + 三层合并 + resolve_api_key（env/file）
75. `renpy_translate.example.json` 示例配置 + `.gitignore` 排除 `renpy_translate.json`
76. main.py argparse 18 个参数改 `default=None` + `--config` 参数 + Config 集成填充
77. 新增 3 个测试（55→58）：config 默认值 / CLI 覆盖 / 配置文件加载
78. README 新增配置文件使用说明章节

### 第十一轮阶段五：Review HTML + 进度增强
79. `ProgressBar` 类加入 `core/translation_utils.py`（~60行）：Unicode/ASCII 自适应 + GBK 安全 + `update()`/`finish()` 接口
80. `tools/review_generator.py` 新增（~150行）：HTML 校对报告（side-by-side + 深色主题 + 按文件折叠 + error/warning/fail 颜色高亮）
81. `translators/direct.py` `run_pipeline` 集成 ProgressBar（非 quiet/dry-run 模式显示）
82. 新增 2 个测试（58→60）：ProgressBar 渲染 + review HTML 生成

### 第十一轮阶段六：类型注解
83. 全部公共 API 补齐返回类型注解（`-> None` / `-> str` 等），修复 `read_file(path)` 和 `update_stats(value)` 参数注解
84. `py.typed` PEP 561 marker 文件
85. CI 新增 mypy 步骤（`continue-on-error: true`，informational）

### 第十一轮阶段七：目标语言参数化
86. `core/lang_config.py` 新增（~130行）：LanguageConfig dataclass + detect_chinese/japanese/korean_ratio + LANGUAGE_CONFIGS（zh/ja/ko/zh-tw）
87. `get_language_config()` 回退到 zh + `resolve_translation_field()` 兼容别名读取
88. main.py 集成：`args.lang_config = get_language_config(args.target_lang)`
89. 新增 3 个测试（60→63）：语言检测 / 配置查找与回退 / 兼容字段读取
90. 中文 prompt 基线 `tests/zh_prompt_baseline.txt` + 零变更验证通过

### 第十一轮阶段八：Prompt + Validator 多语言适配
91. `build_system_prompt` 新增 `lang_config` 参数：zh/zh-tw 走 `_build_chinese_system_prompt`（零变更），其他走 `_build_generic_system_prompt`（英文通用模板）
92. `_GENERIC_SYSTEM_PROMPT_TEMPLATE` 英文通用模板（含 `{field}` 动态 JSON 字段名）
93. `validate_translation` + `_check_quality_heuristics` 新增 `lang_config` 参数：W442 使用 `target_script_detector` + `min_target_ratio` 参数化
94. 新增 3 个测试（63→66）：中文 prompt 零变更回归 / 日语通用模板 / validator lang_config 参数化

### 第十五轮：nvl clear 翻译 ID 修正
178. `fix_nvl_translation_ids` / `fix_nvl_ids_directory`：检测 Ren'Py 8.6+ say-only 哈希 ID，自动修正为 7.x nvl+say 哈希（解决含 `nvl clear` 的翻译块静默失败问题）
179. 管道集成：`run_tl_pipeline` 两处调用点在 `postprocess_tl_directory` 之后自动执行 `fix_nvl_ids_directory`
180. 测试 71→75（`test_fix_nvl_ids_basic` / `no_nvl` / `already_correct` / `real_hashes`）
181. `_clean_rpyc` 增强：新增 .rpymc（编译模块）/ .rpyb（字节码缓存）清理 + 翻译后始终全量清理（不再仅清理已修改文件）

### 第十六轮：screen 文本翻译 + 缓存清理修复
182. `translators/screen.py`（原 screen_translator.py）新增（~420 行）：screen 裸英文翻译（text/textbutton/tt.Action），三组正则 + in_screen 上下文检测 + _should_skip 跳过逻辑 + 去重翻译 + 逐行替换 + .bak 备份 + 断点续传 + dry-run + 42 个内建断言
183. `--tl-screen` CLI 参数 + GUI 勾选框 + 流水线/启动器/打包集成
184. `_clean_rpyc` modified_files 分支修复：增加 .rpymc 精确清理 + .rpyb 全量清理
185. `delete_rpyc_files` 扩展到三种后缀（.rpyc + .rpymc + .rpyb）
186. 资源复制排除列表增加 .rpymc
187. `_RE_NOTIFY` 正则新增：覆盖 `Notify("...")` 弹出通知模式
188. `_should_skip` 修复：先剥离 Ren'Py 标签再检测文件路径（避免 `{/size}` 误杀）
189. 测试 75→87（12 个 screen translator 测试）

### 已回滚
- prompt 强制覆盖指令（CRITICAL RULE）— 降低 AI 返回率 5pp，已撤回

---

## 14. 已知限制 & TODO 队列

### 高优先级
- （无）

### 低优先级
- （无）

### 已完成
- ~~CoT 思维链翻译~~ — 第十三轮（`--cot` flag + 中英双语 addon）
- ~~tl/none 覆盖模板~~ — 第十三轮（none_overlay.rpy 替代直接修改 gui.rpy）
- ~~font_config.json 可配置~~ — 第十三轮（load_font_config + gui_overrides + --font-config CLI）
- ~~pipeline 集成 review_generator~~ — 第十三轮（_run_final_report 自动生成 review.html）
- ~~chunk 失败自动拆分重试~~ — 第十二轮阶段零
- ~~多引擎架构~~ — 第十二轮阶段一~三
- ~~Tkinter GUI~~ — gui.py + build.py PyInstaller 打包
- ~~项目结构整理~~ — 根目录模块迁入 core/ / translators/ / tools/ 三个包
- ~~nvl clear 翻译 ID 修正~~ — 第十五轮（fix_nvl_translation_ids + 管道集成 + 测试 71→75）
- ~~screen 裸英文翻译~~ — 第十六轮（translators/screen.py + --tl-screen + Notify/标签修复 + 流水线集成 + 测试 75→87）
- ~~核心函数单元测试~~ — 87 + 62 + 13 = 162 个用例
- ~~纳入 git + CI~~ — `.github/workflows/test.yml`
- ~~线程安全修复~~ — 并发锁 + ProgressTracker 损坏容错
- ~~回写失败根因分析~~ — 609→28 条（-95%）

---

## 15. 模块间调用关系

```
gui.py (图形界面) ─── start_launcher.py (CLI 菜单) ─── tools/renpy_upgrade_tool.py (独立工具)
       │                      │
       └──────────────────────┘
                │ subprocess 调用
                ▼
main.py (CLI 入口 + --engine 路由)
  ├── [engine=auto/renpy] → translators/ Ren'Py 三条管线
  │    ├── translators/direct.py         (direct-mode)
  │    ├── translators/retranslator.py   (补翻)
  │    ├── translators/tl_mode.py        (tl-mode)
  │    ├── translators/screen.py         (screen 裸英文翻译)
  │    ├── translators/tl_parser.py      (tl/ 解析器)
  │    └── translators/renpy_text_utils.py (文本分析)
  │
  ├── [engine=rpgmaker/csv/jsonl] → engines/ 多引擎架构
  │    ├── engines/engine_detector.py  (检测+路由)
  │    ├── engines/engine_base.py      (EngineProfile/TranslatableUnit/EngineBase)
  │    ├── engines/generic_pipeline.py (6 阶段通用流水线)
  │    ├── engines/rpgmaker_engine.py  (RPG Maker MV/MZ)
  │    └── engines/csv_engine.py       (CSV/JSONL)
  │
  └── core/ 共享基础设施
       ├── core/api_client.py / core/prompts.py / core/glossary.py
       ├── core/translation_db.py / core/translation_utils.py
       ├── core/config.py / core/lang_config.py
       ├── file_processor/ (splitter/patcher/checker/validator)
       ├── tools/font_patch.py / tools/review_generator.py
       └── one_click_pipeline.py → pipeline/ (Ren'Py 四阶段流水线)

tests/   — test_all(87) + test_engines(62) + smoke(13)
tools/   — verify_alignment / revalidate / patch_font_now / analyze_writeback
```

---

## 16. 开发原则

1. **宁可漏翻也不误翻** — 不确定的条目保留原文
2. **数据驱动** — 改动前收集数据，改动后验证数据，不拍脑袋定阈值
3. **隔离变量** — 每次只改一个东西，验证独立效果
4. **不破坏已有功能** — 新功能用开关控制（CLI 参数），默认行为不变
5. **安全优先** — checker 不通过就丢弃、回写前校验、原地操作前备份
6. **先读再写** — 涉及参考项目借鉴时，先完整阅读其源码和文档再给方案
7. **方案先行** — 给出改动方案和受影响函数列表，等用户确认后再写代码
8. **最小改动** — 不做不必要的重构、不加不需要的注释、不改不相关的代码
9. **零依赖** — 坚持纯标准库，不引入第三方包
