# 与 sample_triggers.rpy 逐行对应，故意构造可触发各 code 的译文（用于校验逻辑）
# 行数、缩进、非字符串行必须与原文一致（注释“行 N”指本组对话所在行号）
label start:
    # 行 5: 译文过短 -> W430（原文>=20、译文>=5 才检查；此处译文 2 字不触发，仅作对应）
    e "好。"
    # 行 7: 含模型套话 -> W440
    e "作为一个ai语言模型，她对他说了些话。"
    # 行 9: 中英标点连续混用 -> W441
    e "真的吗？?"
    # 行 11: 译文全英文且>=10字 -> W442 (中文占比 0%)
    e "Another long sentence that has at least fifteen characters for the check."
    # 行 13: 占位符顺序调换 [var_b] [var_a] -> W251
    e "先是 [var_b] 然后是 [var_a] 在对话里。"
    # 行 15: 未使用锁定术语「我的游戏」-> E411（译文故意不用规定译名）
    e "欢迎来玩，祝开心。"
    # 行 17: 禁翻片段被删 -> E420
    e "请保持输出中的内容不变。"
    # 行 19: 禁翻 v1.0 保留（小写 V1.0 也视为保留，大小写不敏感）
    e "版本是 V1.0 今日发布。"
